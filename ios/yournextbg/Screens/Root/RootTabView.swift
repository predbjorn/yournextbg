import SwiftUI

/// Phase 5 placeholder — real five-tab implementation lands in the next
/// commit. Kept here as a stub so `RootView` has somewhere to route
/// signed-in users from Phase 4.
struct RootTabView: View {
    var body: some View {
        Text("Signed in — tab shell coming next.")
            .font(CardstockFont.mono(size: 12))
            .padding()
    }
}
