import SwiftUI

/// Placeholder — full implementation in Phase 6 Tasks 6.9 + 6.10.
struct GameDetailScreen: View {
    let gameId: String

    var body: some View {
        ScreenPlaceholder(title: "Game · \(gameId)")
    }
}
